      - name: Send keepalive POST (curl)
        run: |
          curl -fsS -X POST "${{ secrets.WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -H "X-WEBHOOK-SECRET: ${{ secrets.WEBHOOK_SECRET }}" \
            -d '{"note":"github-actions keepalive"}'
